<nav class="navbar navbar-inverse">
    <div class="container-fluid">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="#">Goal and Get It</a>
      </div>
      <div class="collapse navbar-collapse" id="myNavbar">
        <ul class="nav navbar-nav">
          <li class="active"><a href="#">Home</a></li>
          <li><a href="create_goal">Add New Goal</a></li>
          <li><a href="starter_packs">Starter Pack</a></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
            <li><a href="#"><span class="glyphicon glyphicon-user"></span> Account Settings</a></li>
          <li><a href="#">{{user}} Score<span class="badge"></span> {{score}}</a></li>
        </ul>
      </div>
    </div>
  </nav>
  <div class="container">
    <div class="jumbotron">
      <h1 class="welcome">Welcome Back {{ user }}!!</h1>
    </div>
    <div class="jumbotron">
      <h1 class="currentGoals">Current Goals</h1>
      <hr>
      <ul>
        {{#each goals}}
        <li>{{completeGoals}}</li>
        <li>{{incompleteGoals}}</li>
        {{/each}}
      </ul>
      <h1 class="futureGoals">Future Goals</h1>
      <hr>
      <ul>
        {{#each goals}}
        <li>{{futureGoals}}</li>
        {{/each}}
      </ul>
    </div>
  </div>

